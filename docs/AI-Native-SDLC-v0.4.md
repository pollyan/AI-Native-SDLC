# AI-Native SDLC v0.4 — 规范驱动开发框架设计

> **版本：** v0.4.1
> **日期：** 2026-05-17
> **状态：** 设计迭代中
> **变更说明：** v0.4.1 在 v0.4 基础上加回专家角色维度（§七），采用"工作流驱动 + 角色能力叠加"设计。MVP 不启用独立专家角色，专家作为可选增强层通过配置启用。

---

## 一、概述

### 1.1 什么是 AI-Native SDLC

AI-Native SDLC（AI 原生软件开发生命周期）是一套面向企业开发团队的**规范驱动开发方法论**。它以结构化的 Spec 文件作为阶段间信息传递的统一载体，通过四阶段工作流（Requirement Review → Plan → Execute → Review）驱动 AI 辅助开发全过程，同时确保人始终是决策主体。

核心理念可以概括为三句话：

- **Spec 文件**是阶段间信息传递的唯一载体——结构化、可审查、可版本控制
- **统一语言**是跨阶段信息无损传递的基础设施——术语一致，沟通才有意义
- **垂直切片**是执行阶段的基本单元——端到端、可独立验证、逐步交付

### 1.2 核心理念

| 理念 | 含义 |
|------|------|
| **人主控，AI 辅助** | 人控制所有阶段切换和关键决策；AI 可以在门控条件满足时主动建议，但人最终确认 |
| **Spec 文件作为统一传递载体** | 每个阶段的产出都是一份结构化的 Spec 文件，既是下一阶段的输入，也是本阶段的质量约束 |
| **统一语言作为基础设施** | 项目的领域词汇表（Context）被所有阶段的 AI 行为依赖，术语冲突时实时拦截 |
| **垂直切片驱动执行** | Plan 阶段将方案拆分为端到端的垂直切片，Execute 阶段逐切片串行执行 |

### 1.3 与竞品的差异化定位

详见 §八 与竞品的对比。

---

## 二、产品定位

### 2.1 目标用户

| 角色 | 定义 | 说明 |
|------|------|------|
| **Developer** | 客户企业的一线开发人员 | 框架的主要使用者。在所有阶段中是决策者和操作主体 |
| **FDE** | 现场开发工程师 | 短期内作为框架实施的替身用户，负责客户赋能培训 |

### 2.2 使用场景

以 IDE 插件为主要交互界面（也支持自研 IDE 和 CLI）。工作流阶段的切换在同一个对话框中完成，通过加载不同的角色定义切换 AI 行为。

典型场景：Developer 在 IDE 中收到一个新功能需求，按照工作流从 Requirement Review 开始，逐步经过 Plan → Execute → Review，每个阶段的产出是一份 Spec 文件，最终交付可验证的代码。

### 2.3 交付形态

| 阶段 | 形态 | 说明 |
|------|------|------|
| **短期** | 方法论资产 | 由 FDE 团队在客户现场实施赋能时使用 |
| **中期** | 产品集成 | 集成到 AI Coding 工具中，成为内置的差异化功能 |
| **长期** | 全流程覆盖 | 扩展到完整产研全流程（战略→投注→需求→设计→开发→测试→上线→运维） |

### 2.4 与 v0.3 的关系

详见附录 C。

---

## 三、核心设计原则

### 原则 1：人主控，AI 辅助

- 人控制所有阶段切换
- AI 可以在门控条件明确满足时主动建议进入下一阶段，但人最终确认
- 所有 AI 产出（修复建议、方案推荐等）由人决定是否采纳
- **这是与竞品（如 Devin "AI 自动干、人旁观"模式）的核心差异化**

### 原则 2：Spec 文件作为统一传递载体

- 每个阶段的产出都是一份 Spec 文件
- Spec 既是下一阶段的输入（上下文传递），也是本阶段的质量约束
- Spec 模板 = 方法论的具象化，内置最佳实践
- 通过模板约束内容结构，保证稳定的输出质量

### 原则 2.5：流程优于文档（借鉴 agent-skills）

- Spec 模板应是可执行流程，而非参考文档
- 每个能力点必须有步骤、检查点、退出条件
- 模板的价值在于"填完就知道下一步做什么"，而非"写完再想这是什么意思"

### 原则 3：统一语言作为基础设施

- 项目的领域词汇表（Context）被所有阶段的 AI 行为依赖
- 术语在 Requirement Review 阶段开始建立，后续阶段持续维护
- 术语冲突时实时拦截，发现新术语时立即写入
- **对人：统一语言是锦上添花；对 AI：统一语言是信息无损传递的前提条件**

### 原则 4：垂直切片驱动执行

- Plan 阶段将方案拆分为端到端的垂直切片（不是单个层的水平切片）
- 每个切片贯穿所有集成层（schema + API + UI + tests），可独立测试
- Execute 阶段逐切片串行执行，推荐 TDD 模式（测试→实现→重构）
- **禁止"先写完所有测试再写所有实现"的水平切片反模式**

---

## 四、工作流设计

### 4.1 流程分层

全景视角下，框架覆盖的范围：

```
全景：战略 → 投注 → 需求 → 技术设计 → 开发 → 测试 → 上线 → 运维
                          │                              │
                          └────── MVP 覆盖这段 ──────────┘
```

战略和投注有计划覆盖，但不在 MVP 范围。框架设计已预留扩展位置。

两种流程模式适配不同复杂度的任务：

| 流程 | 阶段组合 | 适用场景 |
|------|---------|---------|
| **Quick** | Execute | 小修复、配置调整、需求极其明确且简单的改动 |
| **Standard** | Requirement Review → Plan → Execute → Review | 所有正常开发任务，完整流程不省略 |

**Quick 模式进入条件（借鉴 Superpowers Scope Check）：**

- 需求只涉及 1 个垂直切片
- 不涉及跨模块改动
- Developer 明确确认走 Quick

**设计理由：** 去除中间档——三档分层过度设计。企业场景要求严格，Standard 不省略任何环节。

**流程推荐机制：**

- **推荐时机**：需求进入系统时
- **判断依据**：需求文档特征 + 项目代码上下文 + Developer 确认
- **默认策略**：信息不足时走 Standard
- **Developer 始终可以**：在任何阶段改变路径
- **回退机制**：允许从后续阶段回退到前序阶段（如 Execute 回退到 Plan 修正切片拆分，Plan 回退到 Requirement Review 补充需求）。回退时保留已完成的产出，不丢弃已完成的工作

### 4.2 四个阶段详解

#### 阶段 1：Requirement Review（需求评审）

| 维度 | 定义 |
|------|------|
| **输入** | 需求文档（PRD/Issue/口头描述，形态不限） |
| **预处理** | 根据输入成熟度走不同路径：完整 PRD → 直接评审；Issue → 轻量补充；口头描述 → 完整结构化 |
| **核心动作** | 结合业务上下文和代码上下文，发现模糊地带、提出精确问题、澄清边界场景。AI 可访问代码库，用于交叉验证需求与现有代码的逻辑一致性 |
| **关键产出** | 验收标准（作为后续 Review 阶段的校验基准） |
| **输出** | 需求 Spec（包含澄清后的需求描述 + 验收标准 + 边界定义 + 术语定义） |
| **AI 行为** | 角色定位：**需求评审助手**。提出精确问题、发现模糊地带、建议验收标准、问题重构——不按用户说的原样理解，而是重新理解“真正要解决什么”（借鉴 gstack） |
| **AI 不做的事** | 不做方案设计、不做技术选型（方案设计在 Plan 阶段进行） |

**模糊地带的四种类型：**

> **追问方法（借鉴 gstack Six Forcing Questions）：** 从六个维度对需求进行追问——Who/What/When/Where/Why/How，确保每个模糊地带都被充分暴露。

| 类型 | 说明 | 需要的信息源 |
|------|------|------------|
| 信息缺失 | 需求压根没提某个边界场景 | 需求文档 + Context |
| 术语歧义 | 用了模糊的词或一词多义 | 需求文档 + Context |
| 逻辑矛盾 | 需求跟现有代码逻辑冲突 | 需求文档 + 代码库 |
| 隐含假设 | 没写但默认了某个前提 | 需求文档 + 业务上下文 |

#### 阶段 2：Plan（方案设计）

| 维度 | 定义 |
|------|------|
| **输入** | 需求 Spec |
| **核心动作** | 技术方案设计 + 垂直切片拆分。当方案涉及不熟悉的技术栈时，先做领域研究（借鉴 GSD RESEARCH.md 机制），研究结果被后续阶段引用 |
| **任务拆分粒度** | 可独立测试的垂直切片（端到端，贯穿所有集成层） |
| **输出** | 方案 Spec（包含技术方案 + 垂直切片列表） |
| **AI 行为** | 角色定位：**方案设计助手**。生成技术方案、拆分垂直切片、识别风险、方案预研对比 |
| **AI 不做的事** | 不做需求质疑（已在上一阶段完成） |

**方案预研**：当方案间存在重大 tradeoff 时，Plan Spec 中包含方案对比，重点回答三个问题：
1. 各方案分别接受了什么代价？
2. 选择 A 意味着放弃了 B 的什么？
3. 什么条件下这个选择需要被推翻？

#### 阶段 3：Execute（执行）

| 维度 | 定义 |
|------|------|
| **输入** | 方案 Spec（垂直切片列表） |
| **执行模式** | 逐切片串行（默认）。根据切片复杂度可选：Subagent-Driven（逐切片分发到独立上下文）vs Inline Execution（批量执行），复杂切片适合前者（借鉴 Superpowers） |
| **推荐方式** | TDD（测试→实现→重构），始终为推荐实践，从不强制。先建信号再做分析——没有可重复的 pass/fail 信号，任何分析都是猜测（借鉴 Matt Pocock 反馈循环优先理念） |
| **输出** | 可运行的代码 |
| **AI 行为** | 角色定位：**编码助手**。逐切片实现、推荐 TDD 实践、运行验证 |
| **AI 不做的事** | 不擅自修改切片范围、不跳过切片。调试时生成 3-5 个可证伪假设，而非锚定在第一个猜测上（借鉴 Matt Pocock） |

**Execute 阶段不产出独立 Spec 文件**，产出是代码，追踪通过 Plan Spec 中的切片列表 checkbox 勾选实现。

#### 阶段 4：Review（评审）

| 维度 | 定义 |
|------|------|
| **审查维度** | ① 代码质量 ② 需求一致性 ③ 验收标准逐条确认 |
| **产出形式** | 问题清单 + 自动修复建议 |
| **决策权** | Developer 决定是否采纳修复建议 |
| **AI 行为** | 角色定位：**评审助手**。三轴审查、问题分级、生成修复建议 |
| **AI 不做的事** | 不自动修复（Developer 决定）、不定义反馈处理协议——人主控模式下，Developer 自己读报告自己判断 |

**三轴审查（借鉴 Matt Pocock 两轴 + 我们的差异化）：**

> **反锚定设计（借鉴 Matt Pocock）：** 三轴独立并行审查，防止互相污染——代码质量审查不受需求一致性判断影响，反之亦然。

| 轴 | 检查什么 | 来源 |
|---|---------|------|
| **A. 代码质量** | 命名、结构、错误处理、安全、性能 | Matt Pocock Standards 轴 |
| **B. 需求一致性** | 实现是否与 Requirement Spec 一致 | Matt Pocock Spec 轴 + Superpowers |
| **C. 验收标准确认** | 逐条检查 Requirement Review 阶段定义的验收标准 | **我们的差异化，竞品都没有** |

### 4.3 阶段转换与门控机制

**统一原则：每个阶段的门控 = 自审核（AI 检查）+ 人工确认（Developer 拍板）**

#### Requirement Review 门控

| # | 自审核检查项 | 来源 |
|---|------------|------|
| 1 | 问题定义清晰 | 设计决策 |
| 2 | 范围界定 | 设计决策 |
| 3 | 验收标准已列出 | 设计决策 |
| 4 | 关键术语已定义 | 设计决策 |
| 5 | 无 placeholder/TBD 残留 | Superpowers |

人工确认：Developer 确认以上内容。

#### Plan 门控

| # | 自审核检查项 | 来源 |
|---|------------|------|
| A1 | 技术方案与 Requirement Spec 一致 | Superpowers cross-ref |
| A2 | 每个切片是端到端垂直切片，可独立测试 | Matt Pocock tracer bullet |
| A3 | 切片依赖关系正确，无循环依赖 | GSD dependencies |
| A4 | 接口合约已定义 | GSD Interface-First |
| A5 | 无 placeholder/TBD 残留 | Superpowers |
| A6 | 每个 Requirement Spec 的功能需求都有切片覆盖 | SpecKit Spec Coverage |

人工确认：Developer 检查自审核结果，对方案做最终判断。

#### Execute 门控

Execute 阶段没有阶段级门控。切片是逐个完成的，每个切片有自己的完成标准：

**切片最小完成标准：**

| # | 条件 | 说明 |
|---|------|------|
| D1 | 切片涉及的功能可运行 | 不是"写完了"，是"跑起来了" |
| D2 | 相关测试通过（如果有的话） | 有测试就必须通过；没有测试不阻断，Review 时标记建议补充 |
| D3 | 无 placeholder/TBD 残留 | 同 Plan 门控，防止假完成 |

**推荐实践（不强制）：**

| # | 条件 | 说明 |
|---|------|------|
| R1 | TDD 循环完成（测试→实现→重构） | 推荐但非门控 |
| R2 | 代码已提交（有意义的 commit） | 可追溯 |
| R3 | 切片未引入已知回归 | 至少跑一下相关测试套件 |

#### Review 门控

| # | 检查项 |
|---|--------|
| 1 | 三轴报告完整 |
| 2 | 所有问题已分级（Critical/Important/Minor） |
| 3 | 验收标准逐条确认 |
| 4 | Developer 确认处理方式 |

---

## 五、Spec 模板

### 5.1 Requirement Review Spec

```markdown
# Requirement Review Spec: [功能名]

## 需求来源
- 输入类型: [完整 PRD / Issue / 口头描述]
- 来源链接: ...

## 需求描述
[澄清后的完整需求描述]

## 术语定义
[本需求涉及的新术语，同步写入 CONTEXT.md]

## 验收标准
- [ ] AC-001: [可验证的验收条件]
- [ ] AC-002: ...
> 注：验收标准将作为 Review 阶段 C 轴的校验基准

## 边界定义
### 包含
- ...
### 不包含
- ...

## 澄清记录
| # | 问题 | 回答 | 来源 |
|---|------|------|------|
| Q1 | ... | ... | AI 提问 / Developer 补充 |

## 自审核检查
- [ ] 问题定义清晰
- [ ] 范围已界定
- [ ] 验收标准已列出
- [ ] 关键术语已定义
- [ ] 无 placeholder/TBD 残留

## Developer 确认
- [ ] 已阅读并确认以上内容
```

**字段说明：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 需求来源 | 是 | 记录输入类型和链接，便于回溯 |
| 需求描述 | 是 | 澄清后的完整描述，不是原始需求的复制粘贴 |
| 术语定义 | Full 必填 | 新发现的领域术语，同步写入 Context 文件 |
| 验收标准 | 是 | 可验证的验收条件，作为 Review 阶段的校验基准 |
| 边界定义 | 是 | 明确包含和不包含的范围 |
| 澄清记录 | 是 | 记录评审过程中提出的问题和回答，便于审计 |
| 自审核检查 | 是 | AI 自动检查，确保 Spec 完整性 |
| Developer 确认 | 是 | 人工确认，人主控原则的体现 |

### 5.2 Plan Spec

```markdown
# Plan Spec: [功能名]

> 关联 Requirement Spec: [链接]

## 技术方案
[方案描述]

## 技术上下文
- 语言/框架: ...
- 主要依赖: ...
- 存储方案: ...
- 性能要求: ...
- 约束条件: ...

## 方案预研（可选：当方案间存在重大 tradeoff 时填写）
### Option A: [方案名]
- 接受的代价: ...
- 放弃了什么: ...
- 推翻条件: ...
### Option B: [方案名]
- 接受的代价: ...
- 放弃了什么: ...
- 推翻条件: ...
> 选择理由: ...
> 推翻信号: ...

## 垂直切片列表
| # | 切片名 | 涉及文件 | 依赖 | 测试方式 |
|---|--------|---------|------|---------|
| S1 | ... | ... | - | ... |
| S2 | ... | ... | S1 | ... |

## 风险识别（可选）
| # | 风险 | 影响 | 应对 |
|---|------|------|------|
| R1 | ... | ... | ... |

## 自审核检查
- [ ] 技术方案与 Requirement Spec 一致
- [ ] 每个切片是端到端垂直切片，可独立测试
- [ ] 切片依赖关系正确，无循环依赖
- [ ] 接口合约已定义
- [ ] 无 placeholder/TBD 残留
- [ ] 每个 Requirement Spec 功能需求都有切片覆盖

## Developer 确认
- [ ] 已阅读并确认以上内容
```

**字段说明：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 技术方案 | 是 | 核心方案描述，清晰说明"怎么做" |
| 技术上下文 | 是 | 技术栈和环境信息，便于 Execute 阶段准确编码 |
| 方案预研 | 可选 | 当方案间存在重大 tradeoff 时填写，重点是代价、放弃和推翻条件 |
| 垂直切片列表 | 是 | 端到端切片，每个切片可独立测试。依赖关系正确，无循环 |
| 风险识别 | 可选 | 已知风险和应对方案 |
| 自审核检查 | 是 | 6 项自动检查，确保方案质量和完整性 |
| Developer 确认 | 是 | 人工确认 |

**垂直切片的设计原则（详见 §三 原则 4 和 §4.2 阶段 3）：**

- 每个切片是端到端的垂直切片（schema + API + UI + tests），不是单个层的水平切片
- 切片可独立测试、可独立验证
- 依赖关系显式声明，无循环依赖
- **禁止"先写所有 schema → 再写所有 API → 再写所有 UI"的水平切片**

### 5.3 Review Report

```markdown
# Review Report: [功能名]

> 关联 Requirement Spec: [链接]
> 关联 Plan Spec: [链接]

## 审查总结
- 审查切片数: ...
- 发现问题数: Critical [N] / Important [N] / Minor [N]
- 综合评估: [通过 / 需修复后通过 / 需重大返工]

## A 轴：代码质量
### Strengths
- ...
### Issues
[按 Critical / Important / Minor 分组]

## B 轴：需求一致性
### 与 Requirement Spec 对照
- ...

## C 轴：验收标准逐条确认
| # | 验收标准 | 状态 | 备注 |
|---|---------|------|------|
| AC-001 | ... | ✅ 通过 / ❌ 未通过 | ... |

## 推荐修复项（自动生成，Developer 决定是否采纳）
- [ ] [修复项 1]
- [ ] [修复项 2]

## Developer 确认
- [ ] 已阅读审查报告，决定如下：...
```

**字段说明：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 审查总结 | 是 | 审查范围和问题统计，给出综合评估 |
| A 轴：代码质量 | 是 | 命名、结构、错误处理、安全、性能方面的检查 |
| B 轴：需求一致性 | 是 | 实现与 Requirement Spec 的一致性对照 |
| C 轴：验收标准确认 | 是 | 逐条确认 Requirement Review 阶段定义的验收标准 |
| 推荐修复项 | 是 | 自动生成的修复建议，Developer 决定是否采纳 |
| Developer 确认 | 是 | 人工确认处理方式 |

**问题分级标准：**

| 级别 | 定义 | 处理要求 |
|------|------|---------|
| **Critical** | 功能缺陷、数据安全、上线阻断 | 当前切片必须修复后才能继续 |
| **Important** | 质量隐患、设计不合理、测试不足 | 建议修复，Developer 决定是否采纳 |
| **Minor** | 风格、命名、微小改进 | 记录即可，不阻断 |

**问题标准格式**（每个问题必须包含）：

```
- 级别（Critical/Important/Minor）
- 问题标题
- 位置：file:line
- 为什么重要
- 建议修复（自动生成，Developer 决定是否采纳）
```

---

## 六、Context 管理

### 6.1 术语表机制

项目的领域词汇表（Context 文件）是跨阶段的**基础设施层**，所有阶段的 AI 行为必须遵守 Context 中的术语定义。

**为什么统一语言对 AI 是基础设施：**

人可以模糊沟通——说"把这个订单处理一下"，对方能推断是 Order 还是 Fulfillment。但 AI 不具备这种隐性推理能力。如果 Requirement Review 阶段说"购买请求"，Plan 阶段说 "Purchase"，Execute 阶段用 `Transaction`，Review 阶段说"订单"——四个阶段之间的信息传递会逐步积累偏差。

### 6.2 维护触发点

**维护原则：AI 写入，人确认。**

| # | 触发点 | 动作 | 阶段 |
|---|--------|------|------|
| **T1** | Requirement Review 中首次确定领域术语 | 创建/追加术语定义 | Requirement Review |
| **T2** | 后续阶段发现术语冲突或歧义 | 拦截并提示澄清，更新定义 | 任意阶段 |
| **T3** | 新概念出现（之前没涉及的领域） | 追加术语，标记来源阶段 | 任意阶段 |
| **T4** | 术语含义发生变更（需求演化） | 更新定义，标记变更历史 | 任意阶段 |

**核心原则（借鉴 Matt Pocock）：**

- **懒创建**：不需要预先手动建表，在第一次确定术语时自动生成
- **实时维护**：术语被确定时立即写入，不批量处理
- **单文件存储**：便于 AI 一次性加载

### 6.3 存储形式

Context 文件存储在项目根目录的 `CONTEXT.md`，格式如下：

```markdown
# [项目名] Context

[一两句话说明这是什么项目]

## Language

**Order**:
客户提交的购买请求，包含一个或多个 LineItem
_Avoid_: Purchase, transaction, 购买请求

**Invoice**:
发货后发送给客户的付款请求
_Avoid_: Bill, payment request, 账单

## Relationships

- 一个 **Order** 产生一个或多个 **Invoice**
- 一个 **Invoice** 属于恰好一个 **Customer**

## Flagged ambiguities

- "account" 被同时用来指 Customer 和 User — 已解决：这是两个不同概念
```

**格式规则：**

- 每个术语定义**一句话**，定义它"是什么"，不定义它"做什么"
- 同义词标记为 `_Avoid_`，明确"应该避免的别名"
- `Relationships` 用加粗术语名 + 基数表达
- `Flagged ambiguities` 记录历史上出现过的术语冲突及解决结果
- 只包含项目特有的领域术语，不含通用编程概念

### 6.4 活跃/归档分离（借鉴 OpenSpec）

活跃任务和已完成任务用不同目录管理，防止 `.agents/` 下堆积大量历史产出影响当前工作。

```
.agents/
├── active/                    # 活跃任务（当前正在进行的）
│   └── {task-name}/
│       ├── requirement-spec.md
│       ├── plan-spec.md
│       └── status.yaml
│
├── archive/                   # 已完成任务（归档）
│   └── 2026-05-17-{task-name}/
│       ├── requirement-spec.md
│       ├── plan-spec.md
│       └── review-report.md
│
├── CONTEXT.md                 # 领域词汇表
├── config.yaml                # 项目配置
├── workflows/                 # 工作流定义
├── roles/                     # 角色定义
└── log.jsonl                  # 可观测性日志
```

**任务状态流转：**

```yaml
# active/{task-name}/status.yaml
status: plan  # draft → requirement-review → plan → execute → review → done
stage: plan
date_started: 2026-05-17
date_updated: 2026-05-17
context_budget_used: 0.35
```

当一个任务完成后：
1. 状态标记为 `done`
2. 整个文件夹从 `active/` 移到 `archive/`，加上日期前缀
3. 关键经验（如果有）沉淀到项目配置

**好处：**
- `active/` 只有当前在做的任务，一目了然
- `archive/` 保留完整历史，带日期前缀方便查找
- 可以同时有多个活跃任务，互不干扰

### 6.5 上下文预算管理（借鉴 GSD）

**核心洞察：AI 编程质量随上下文使用量递减。**

| 上下文使用量 | 质量 | AI 状态 |
|-------------|------|--------|
| 0-30% | 最高 | 全面、细致 |
| 30-50% | 良好 | 自信、扎实 |
| 50-70% | 下降 | 开始走捷径 |
| 70%+ | 较差 | 敷衍了事 |

**每个工作阶段应控制在一个合理的上下文预算内。**

**指导原则：**

- 单个阶段建议消耗 < 50% 上下文
- 如果 Plan 产出超过 30%，建议拆分为多个 Execute 轮次
- Execute 阶段的每个切片建议 10-30% 上下文
- 如果对话接近 70%，建议保存产出、归档当前状态、开启新对话继续

**实现方式：** 这不是精确度量（LLM 不暴露实际 token 使用量），而是定性指导信号。AI 角色在执行过程中自我评估上下文消耗，人在对话中观察到质量下降时主动要求拆分。

在 `status.yaml` 中追踪估算值，供跨会话参考。

### 6.6 配置分层

**项目层（随代码库版本控制）定义"团队共识"：**

```yaml
# .agents/config.yaml
project:
  name: "my-project"
  tech_stack: ["typescript", "react", "node"]
  code_style: "eslint-config-airbnb"

workflows:
  active:
    - feature
  default: feature

roles:
  active:
    - requirement-reviewer
    - planner
    - developer
    - reviewer

gates:
  plan: required       # Plan 结束后必须人工确认
  execute: auto         # Execute 结束后可自动流转
  review: required      # Review 结束后必须人工确认
```

**个人层（本地，不进入版本控制）定义"个体偏好"：**

```yaml
# ~/.agents/config.yaml
preferences:
  communication_style: concise
  default_model: sonnet
  tdd_preferred: true
```

**核心价值：团队共识和个体偏好解耦。** 项目配置可迁移（换人接手自然继承），个人配置有主权（不随项目传播）。

### 6.7 结构化日志（log.jsonl）

多阶段工作流最大的调试坑是出了问题不知道问题出在哪个环节。最轻量的解决方案：

```jsonl
{"ts":"2026-05-17T14:30:00+08:00","stage":"requirement-review","action":"spec-created","output":"requirement-spec.md","status":"done","duration_s":180}
{"ts":"2026-05-17T14:35:00+08:00","stage":"plan","action":"spec-created","output":"plan-spec.md","status":"done","duration_s":300}
{"ts":"2026-05-17T15:30:00+08:00","stage":"execute","action":"slice-complete","output":"S1","status":"done","duration_s":900}
{"ts":"2026-05-17T15:50:00+08:00","stage":"review","action":"report-created","output":"review-report.md","status":"issues_found","duration_s":180}
```

**字段定义：**

| 字段 | 说明 |
|------|------|
| `ts` | 时间戳 |
| `stage` | 当前工作流阶段 |
| `action` | 具体动作（spec-created / slice-complete / report-created 等） |
| `output` | 产出的文件或切片 ID |
| `status` | `done` / `issues_found` / `blocked` / `skipped` |
| `duration_s` | 耗时（秒） |

**不需要可视化 dashboard，但至少能 `tail -f` 看流转状态。**

### 6.8 模型路由

不同阶段可以运行在不同的 AI 模型上，优化成本和质量：

| 阶段 | 模型倾向 | 原因 |
|------|---------|------|
| Requirement Review | 中等推理模型 | 需要理解力和追问能力 |
| Plan | 强推理模型（Opus 级） | 需要深度思考和多维度权衡 |
| Execute | 快速编码模型（Sonnet 级） | 吞吐量优先，上下文干净 |
| Review | 专项模型 | 覆盖率和准确性优先 |

**实现依赖：** 需要底层工具支持模型指定能力。在 `config.yaml` 中配置默认模型映射，Developer 可以在执行时覆盖。

---

## 七、专家角色体系

### 7.1 设计理念：工作流驱动 + 角色能力叠加

框架采用**双维度设计**：

1. **工作流维度**：四阶段主线（Requirement Review → Plan → Execute → Review），每个阶段有明确的输入、输出、门控
2. **角色维度**：专家角色在关键节点提供多视角审查，作为可选的增强层

**核心原则：**
- 工作流主线必须能独立运行（MVP 不依赖专家角色）
- 专家角色是可选的增强层，通过配置启用
- 专家角色不改变工作流主线的阶段划分和门控条件
- 同一个专家角色可以跨阶段复用（如架构师在 Plan 和 Review 都参与）

**竞品参考：**
- gstack：CEO Review / Eng Review / Design Review — 不同视角的独立审查
- bmad：PM / Architect / UX Designer / Dev / QA — 完整的专家角色体系
- gsd：Researcher / Simplifier / Boundary Keeper / Failure Analyst — 访谈视角轮换

### 7.2 专家角色定义

| 角色 | 核心视角 | 参与阶段 | MVP | 未来 |
|------|---------|---------|-----|------|
| **产品分析师** | 需求完整性、用户场景覆盖、业务价值 | Requirement Review | ❌ | ✅ v1.1 |
| **架构师** | 技术可行性、方案合理性、系统影响 | Plan、Review | ❌ | ✅ v1.1 |
| **测试专家** | 验收标准可测试性、边界条件覆盖、回归风险 | Requirement Review、Review | ❌ | ✅ v1.2 |
| **代码审查员** | 代码质量、安全、性能 | Review | ❌ | ✅ v1.2 |
| **DevOps 工程师** | 构建、部署、监控、回滚 | Plan、Review | ❌ | ✅ v1.3 |

### 7.3 MVP 策略

**MVP 阶段不启用独立专家角色。** 原因：
1. 降低认知负担——Developer 只需要理解工作流主线
2. 降低实现复杂度——不需要多角色调度和上下文传递
3. 验证主流程——先确保工作流本身跑通，再加角色

**MVP 中，每个阶段的主流程已经内置了基础的多视角检查：**

| 阶段 | 内置检查（替代独立专家） |
|------|----------------------|
| Requirement Review | 自审核 5 项检查（问题定义/范围/验收标准/术语/无 TBD） |
| Plan | 自审核 6 项检查（方案一致/切片端到端/依赖正确/接口定义/无 TBD/需求覆盖） |
| Review | 三轴审查（代码质量 + 需求一致性 + 验收标准逐条确认） |

### 7.4 专家角色的启用方式

专家角色通过配置文件启用，不是硬编码在 Skill 里：

```yaml
# config.yaml（项目级配置）
experts:
  # 是否启用专家角色（默认 false）
  enabled: false
  
  # 启用哪些角色
  roles:
    - product-analyst    # 产品分析师
    - architect          # 架构师
    # - test-expert      # 测试专家（注释掉 = 不启用）
    # - code-reviewer    # 代码审查员
    
  # 每个角色的参与时机
  triggers:
    product-analyst:
      phase: requirement-review
      point: after-self-review  # 在自审核之后触发
    architect:
      phase: [plan, review]
      point: before-gate  # 在门控之前触发
```

### 7.5 专家角色的工作方式

专家角色以**审查报告**的形式输出，不直接修改 Spec 文件：

```
专家角色审查流程：
1. 读取当前阶段的 Spec 文件
2. 从专家视角进行分析
3. 产出审查报告（问题清单 + 建议）
4. Developer 决定是否采纳建议
5. 如果采纳，主流程 AI 执行修改
```

**与竞品的差异：**
- bmad 的专家是独立的 Agent，有独立的上下文和决策权
- 我们的专家是**顾问模式**——输出建议，Developer 决定
- 这符合"人主控"的核心理念

### 7.6 AI 行为边界

**所有角色（主流程 AI + 专家角色）的共同约束：**

1. 使用 Context 文件中的统一术语
2. 每个阶段产出后执行自审核检查
3. 不跳过门控条件
4. 遇到不确定的内容使用 `[NEEDS CLARIFICATION]` 标记，不猜测
5. 不问"是否继续"——Developer 要求执行就执行到底

**物理实现：** 所有角色都是 `.md` 文件定义的身份，在同一个 IDE 对话进程中被先后加载。不存在独立进程、独立上下文或"换人"的问题——从头到尾就是同一个对话，只是在不同阶段加载不同的 `.md` 来切换行为。

---

## 八、与竞品的对比

### 8.0 差异化定位（原 §一.3）

当前 AI 编程工具/框架市场存在两类极端：

- **Devin / Qoder 模式**：AI 自动干、人旁观——AI 自主执行，人类只在必要时介入
- **传统编码工具**：人全干、AI 打下手——AI 只是代码补全和问答

AI-Native SDLC 选择的是**中间路线**：

> 人是决策主体，AI 是专业助手。每个阶段由人控制和确认，AI 提供专业能力支持，但最终方案选择、质量判断、阶段切换全部由人决定。

这个定位与 Superpowers（obra/superpowers，183K stars）的理念最为接近，但我们在以下方面做了关键升级：

| 维度 | Superpowers | AI-Native SDLC v0.4 |
|------|-------------|---------------------|
| 验收标准 | 无显式追踪 | Requirement Review 阶段定义验收标准，Review 阶段逐条确认 |
| 统一语言 | 无内建机制 | Context 文件作为基础设施，四阶段持续维护 |
| 执行粒度 | Task（可水平切片） | 垂直切片强制——端到端、可独立验证 |
| Review 维度 | 两轴（代码质量 + Spec 合规） | 三轴（代码质量 + 需求一致性 + 验收标准逐条确认） |

### 8.1 设计理念对比

| 维度 | Superpowers | SpecKit | GSD | BMAD | AI-Native SDLC v0.4 |
|------|-------------|---------|-----|------|---------------------|
| **核心隐喻** | 技能驱动 | 声明式工作流 | Goal-Backward | 角色驱动 | **规范驱动** |
| **AI 定位** | 辅助 | 辅助 | 辅助 | 执行主体 | **辅助，人主控** |
| **阶段间传递** | 文件 | 文件 + Gate | STATE.md | 多文件层级 | **Spec 文件统一传递** |
| **门控机制** | HARD-GATE | review gate | 自动检测 | IR Check | **自审核 + 人工确认双层** |
| **术语管理** | 无 | 无 | 无 | config.yaml | **Context 文件基础设施** |
| **执行粒度** | Task | Phase + Task | Plan + Task | Epic → Story | **垂直切片** |
| **Review 维度** | 两阶段 | Gate 检查 | 三层验证 | QA Checklist | **三轴审查** |

### 8.2 功能特性对比

| 特性 | Superpowers | SpecKit | GSD | BMAD | OpenSpec | AI-Native SDLC |
|------|:-----------:|:-------:|:---:|:----:|:--------:|:--------------:|
| 验收标准追踪 | ❌ | ✅ | ✅ | ✅ | ❌ | ✅ |
| 验收标准逐条确认 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅（差异化） |
| 统一语言基础设施 | ❌ | ❌ | ❌ | ⚠️ | ❌ | ✅（差异化） |
| 垂直切片强制 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅（差异化） |
| 自审核循环 | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 方案预研（tradeoff） | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 需求 ID 编号 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 流程分层 | ❌ | ❌ | ❌ | ⚠️ | ❌ | ✅ |
| 活跃/归档分离 | ❌ | ❌ | ⚠️ | ❌ | ✅ | ✅ |
| Spec 模板 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 自动修复建议 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅（差异化） |
| Context 质量管理 | ❌ | ❌ | ✅ | ❌ | ❌ | ✅ |

### 8.3 我们的差异化优势

综合对比，AI-Native SDLC v0.4 相对所有竞品的三个核心差异化：

| # | 差异化 | 说明 |
|---|--------|------|
| 1 | **验收标准闭环** | Requirement Review 定义验收标准 → Review 阶段逐条确认。竞品中无人实现这个闭环 |
| 2 | **统一语言基础设施** | Context 文件作为跨阶段基础设施，术语冲突实时拦截。竞品中仅 BMAD 有弱版本 |
| 3 | **三轴审查 + 自动修复建议** | 代码质量 + 需求一致性 + 验收标准确认，三级问题分级，自动生成修复建议。竞品最多做到两轴 |

---

## 九、演进路线

### 9.1 MVP 范围（v0.4）

| # | 功能 | 状态 |
|---|------|------|
| 1 | 四阶段工作流（Requirement Review → Plan → Execute → Review） | ✅ 已设计 |
| 2 | 两种流程分层（Quick / Standard） | ✅ 已设计 |
| 3 | Spec 模板（Requirement Review Spec / Plan Spec / Review Report） | ✅ 已设计 |
| 4 | 门控机制（自审核 + 人工确认） | ✅ 已设计 |
| 5 | Context 文件（领域词汇表） | ✅ 已设计 |
| 6 | AI 角色定义（四阶段行为规范） | ✅ 已设计 |
| 7 | 垂直切片执行策略 | ✅ 已设计 |
| 8 | 三轴审查 + 问题分级 | ✅ 已设计 |

> **MVP 约束：** MVP 阶段不引入 ADR（Architecture Decision Record）机制，设计决策记录已在设计文档中维护。ADR 将在短期增强阶段评估引入。

### 9.2 短期增强

| # | 功能 | 借鉴来源 | 说明 |
|---|------|---------|------|
| 1 | Anti-Rationalization 机制 | agent-skills | 在关键决策点预设 AI 常见偷懒借口和反驳 |
| 2 | Doubt-Driven Development 协议 | agent-skills | Review 阶段引入对抗性审查 |
| 3 | Source-Driven Development 能力 | agent-skills | 编码前从官方文档获取并引用来源 |
| 4 | 声明式工作流定义（YAML） | SpecKit | 工作流从 Markdown 转为 YAML 声明式定义 |
| 5 | Constitution 机制 | SpecKit | 项目级"宪法"文件，定义不可违反的质量标准 |
| 6 | 编排模式分类文档 | agent-skills | 明确支持和不支持的编排模式 |

### 9.3 长期扩展方向

| # | 功能 | 借鉴来源 | 说明 |
|---|------|---------|------|
| 1 | 覆盖完整产研全流程 | — | 战略→投注→需求→设计→开发→测试→上线→运维 |
| 2 | 三层配置合并 | BMAD | base → team → user 三层配置，支持标量覆盖、表深度合并 |
| 3 | 角色 persistent_facts | BMAD | 每个角色记忆持久事实（技术栈选型、项目约束等） |
| 4 | 增量实现（Incremental Implementation） | agent-skills | 每个切片 ~100 行，三种切片策略 |
| 5 | 上下文工程（Context Engineering） | agent-skills | 定义角色在每个阶段的上下文加载策略 |
| 6 | 多维度评审 | gstack | CEO/设计/工程/DX 多视角评审 |
| 7 | Action-based 工作流 | OpenSpec | 非线性工作流，随时可回退、可更新 |
| 8 | Wave-based 并行执行 | GSD | 无依赖的切片并行执行 |
| 9 | 安全审计 | gstack | OWASP Top 10 + STRIDE 威胁建模 |
| 10 | 跨会话学习 | gstack + GSD | 跨会话的经验记录和参考 |
| 11 | 上下文预算管理 | v0.3 原有设计 | 动态监控 token 使用，接近上限时触发压缩或归档，确保每个阶段的上下文在可控范围内 |
| 12 | 结构化日志（log.jsonl） | v0.3 原有设计 | 记录每个阶段的输入输出、决策点、门控结果，便于审计和调试 |
| 13 | 模型路由 | v0.3 原有设计 | 根据任务复杂度和成本约束，动态选择合适的模型（如 Requirement Review 用轻量模型，Plan 用重量模型） |
| 14 | 自演进机制 | v0.3 原有设计 | 从历史会话中学习，优化 Spec 模板和门控条件 |
| 15 | 多模态支持 | — | 需求输入支持原型图、流程图、视频演示等非文本形式 |
| 16 | Delta Spec 增量规范 | OpenSpec | 只描述变化不全量重写，减少 token 消耗 |

---

## 附录

### 附录 A：术语表

> 完整术语表见项目根目录 [CONTEXT.md](../CONTEXT.md)。以下为核心术语摘要。

| 术语 | 定义 |
|------|------|
| **Requirement Review** | 需求评审阶段。对输入的需求文档进行评审，发现模糊地带、提出精确问题、澄清边界场景，产出包含验收标准的需求 Spec |
| **Plan** | 方案设计阶段。基于需求 Spec 进行技术方案设计和垂直切片拆分，产出方案 Spec |
| **Execute** | 执行阶段。逐切片串行执行，推荐 TDD 模式但不强制 |
| **Review** | 评审阶段。从代码质量、需求一致性、验收标准三个维度审查，产出问题清单和修复建议 |
| **Spec** | 一个阶段的结构化产出文件，同时作为下一阶段的输入 |
| **Developer** | 使用本框架的一线开发人员，在所有阶段中是决策者和操作主体 |
| **FDE** | 现场开发工程师，短期内作为框架实施的替身用户 |
| **Context** | 项目的领域词汇表，定义项目特有的业务术语及其关系 |
| **Vertical Slice** | 可独立测试的端到端任务单元，贯穿所有集成层 |
| **门控（Gate）** | 阶段转换的质量检查点，由自审核（AI）+ 人工确认（Developer）组成 |

### 附录 B：竞品参考来源

| 框架 | GitHub 地址 | Stars | 核心借鉴点 |
|------|------------|-------|-----------|
| Superpowers | github.com/obra/superpowers | 183K | HARD-GATE、自审核循环、方案对比、No Placeholders |
| SpecKit | github.com/github/spec-kit | 94K | Constitution Check、Spec Coverage、NEEDS CLARIFICATION 标记 |
| gstack | github.com/garrytan/gstack | 92K | 多维度评审、Six Forcing Questions |
| GSD | github.com/gsd-build/get-shit-done | 61K | Goal-Backward、YAML Frontmatter、三层验证 |
| BMAD | github.com/bmad-code-org/BMAD-METHOD | 47K | 角色驱动、三层配置合并、FR/NFR 分离 |
| OpenSpec | github.com/Fission-AI/OpenSpec | 46K | Delta Spec、活跃/归档分离、RFC 2119 关键词 |
| specs.md | github.com/fabriqaai/specs.md | 142 | 自适应检查点、多流程可选 |
| Matt Pocock Skills | mattpocock.com | — | 两轴评审、垂直切片、CONTEXT.md 词汇表、拷问式审查 |
| agent-skills | github.com/addyosmani/agent-skills | 新 | Anti-Rationalization、Doubt-Driven、Source-Driven |

### 附录 C：设计决策历史

#### v0.3 vs v0.4 对比（原 §二.4）

v0.4 是在 v0.3 基础上的**重大升级**：

| 维度 | v0.3（多智能体专家团） | v0.4（规范驱动开发框架） |
|------|----------------------|------------------------|
| 核心隐喻 | "多智能体专家团" | "规范驱动开发" |
| 阶段命名 | Brainstorm → Plan → Execute → Review | **Requirement Review** → Plan → Execute → Review |
| 信息载体 | 对话上下文为主 + 文件快照为辅 | **Spec 文件为统一传递载体** |
| 验收标准 | 无显式机制 | Requirement Review 阶段定义，Review 阶段逐条确认 |
| 统一语言 | 无内建机制 | **Context 文件作为基础设施** |
| 执行策略 | Task List（无切片约束） | **垂直切片强制** |
| 门控机制 | 软检查点 | **自审核（AI）+ 人工确认（Developer）双层门控** |
| Spec 模板 | 无正式模板 | **每个阶段有完整的 Markdown 模板** |

#### 版本历史

| 版本 | 核心变更 | 日期 |
|------|---------|------|
| v0.1 | 初始版本，8 专家 + 2 调度的角色驱动架构 | 2026-05-07 |
| v0.2 | 角色精简为 4 核心，信息流转重定义，自演进简化 | 2026-05-08 |
| v0.3 | 架构重构为工作流驱动 + 角色能力双层，新增活跃/归档分离、流程分层、上下文预算管理 | 2026-05-09 |
| v0.3.1 | 新增活跃/归档分离、轻量/重量流程、上下文预算管理 | 2026-05-09 |
| **v0.4** | **演化为"规范驱动开发框架"：阶段重命名、Spec 文件统一传递、验收标准闭环、统一语言基础设施、垂直切片强制、门控机制强化、完整 Spec 模板、三轴审查** | **2026-05-17** |

---

*本文档基于 v0.4 设计决策记录，综合 7 个竞品框架对比分析和 Matt Pocock Skills 增强分析产出。*
